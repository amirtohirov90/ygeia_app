import 'package:flutter/material.dart';

class YgeiaColors {
  YgeiaColors._();

  // Backgrounds
  static const Color bgBase = Color(0xFFFAF7F2);
  static const Color bgCard = Color(0xFFF2EDE3);
  static const Color bgCardElevated = Color(0xFFEBE4D6);

  // Text
  static const Color textPrimary = Color(0xFF2C2C2C);
  static const Color textSecondary = Color(0xFF6B6157);
  static const Color textMuted = Color(0xFFA89B8E);

  // Accent (sage green)
  static const Color accent = Color(0xFF5B7F6A);
  static const Color accentSoft = Color(0x265B7F6A); // accent 15% opacity
  static const Color accentSecondary = Color(0xFFC97B5C); // warm terracotta

  // Structural
  static const Color divider = Color(0xFFE5DDD0);
  static const Color white = Colors.white;
}
